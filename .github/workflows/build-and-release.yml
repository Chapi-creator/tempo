name: build-and-release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: desktop/package-lock.json
      - name: Audit de dependencias
        working-directory: desktop
        run: npm audit --audit-level=high
      - name: Tests (modelo + seguridad)
        run: node test.js
      - name: Instalar deps
        working-directory: desktop
        run: npm ci
      - name: Aplicar fuses de Electron
        working-directory: desktop
        run: npm run fuses
      - name: Build instalador
        working-directory: desktop
        run: npm run dist
      - name: Publicar Release + feed de auto-update (idempotente)
        working-directory: desktop
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $exists = gh release view $tag 2>$null
          if ($exists) {
            Write-Host "Release $tag ya existe, actualizando assets (tolerante a re-runs)"
            gh release upload $tag "release\Tempo Setup $tag.exe" "release\Tempo Setup $tag.exe.blockmap" "release\latest.yml" --clobber
          } else {
            gh release create $tag "release\Tempo Setup $tag.exe" "release\Tempo Setup $tag.exe.blockmap" "release\latest.yml" --title "Tempo $tag" --notes "Build audited, tests passed, fuses enabled."
          }
        env:
          GH_TOKEN: ${{ github.token }}